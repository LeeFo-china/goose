{
  "name": "@gooes/domain",
  "version": "1.18.0",
  "type": "module",
  "private": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "sideEffects": false,
  "dependencies": {
    "zod": "^4.4.2"
  },
  "scripts": {
    "clean": "rm -rf dist *.tgz",
    "build": "bun build ./src/index.ts --outdir ./dist --target node --format esm --external zod && tsc -p tsconfig.json && node ./scripts/verify-build.mjs",
    "verify:packed-consumer": "node ./scripts/verify-packed-consumer.mjs",
    "prepack": "bun run clean && bun run build"
  },
  "publishConfig": {
    "access": "restricted"
  }
}
