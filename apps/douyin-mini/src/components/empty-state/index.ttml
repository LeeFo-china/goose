<view class="empty-state">
  <view class="empty-mark" aria-hidden="true"></view>
  <view class="empty-title">{{title}}</view>
  <view class="empty-description">{{description}}</view>
  <button tt:if="{{actionLabel}}" class="empty-action" bindtap="onAction">{{actionLabel}}</button>
</view>
