# Admin 列表页布局整改摘要

日期：2026-05-12

## 目标

后台列表页统一为“页面概览 + 列表 Card”的两段式结构，避免筛选、说明、入口和表格分散在页面多个区域。

## 推荐结构

1. 页面顶部：标题、页面说明、主操作按钮。
2. 指标区：展示当前页面最重要的 3-6 个摘要指标。
3. 列表 Card：
   - `CardHeader`：列表标题、当前筛选摘要、右侧状态 Badge。
   - `CardHeader` 内第二行：筛选条件、搜索框、状态选择。
   - `CardContent`：表格。
   - `CardContent` 底部：分页和总数。

## 客户管理页整改

已将客户管理页调整为：

- 顶部保留客户管理标题和新增客户按钮。
- 指标区展示当前筛选客户、本页有效客户、本页跟进/到店、本页可见号码。
- 删除独立筛选 Card。
- 将客户状态、来源、渠道、跟进状态、关键词搜索放入“客户列表” Card 头部。
- 分页放入客户列表 Card 底部，和表格形成完整列表上下文。

## 项目管理页整改

已将项目管理页调整为：

- 顶部保留项目管理标题和新增项目按钮。
- 指标区展示当前筛选项目、本页有效项目、本页施工/验收、本页预算。
- 删除独立筛选 Card。
- 将项目状态、可见范围、关键词搜索放入“项目列表” Card 头部。
- 分页放入项目列表 Card 底部，和项目表格形成完整列表上下文。

## 费用审批页整改

已将费用审批页调整为：

- 顶部保留费用审批标题和页面说明。
- 指标区展示当前筛选费用、本页金额、本页审批中、本页待打款。
- 删除独立筛选 Card。
- 将费用状态、模式、审批节点、关键词、创建日期范围和重置操作放入“费用申请列表” Card 头部。
- 分页放入费用申请列表 Card 底部，和费用申请表格形成完整列表上下文。
- 同步调整 `loading.tsx`，保持两段式骨架和真实页面一致。

## 租户营销活动页整改

已将租户营销活动页调整为：

- 顶部保留营销活动标题、页面说明和当前 tab 的主操作按钮。
- 保留活动管理、H5 活动页、H5 营销线索三个 tab，tab 仍作为业务分区入口。
- 活动管理 tab 删除独立筛选 Card，将活动类型、活动状态和关键词搜索放入“活动列表” Card 头部。
- 活动管理 tab 分页放入活动列表 Card 底部，和活动表格形成完整查询上下文。
- H5 营销线索删除独立筛选 Card，将线索状态、活动页、关键词、提交日期范围和重置操作放入“H5 营销线索” Card 头部。
- H5 营销线索分页放入线索列表 Card 底部，和线索表格形成完整查询上下文。
- H5 活动页无筛选分页，保留单列表 Card，并统一使用 `CardDescription` 承载说明。
- 同步调整 `loading.tsx`，补齐 tab 骨架，并移除独立筛选骨架 Card。

## 租户工地监控页整改

已将租户工地监控页调整为：

- 顶部保留工地监控标题、页面说明和绑定摄像头主操作。
- 指标区保留已绑定项目、未绑定设备通道、隐藏摄像头三个核心指标。
- 保留项目摄像头、设备接入两个 tab，tab 仍作为业务分区入口。
- 项目摄像头 tab 删除独立搜索 Card，将关键词搜索、清除操作和当前页码放入“项目摄像头” Card 头部。
- 项目摄像头分组改为列表 Card 内的轻量边框区块，避免 Card 嵌套造成层级过重。
- 项目摄像头分页放入列表 Card 底部，和项目摄像头分组形成完整查询上下文。
- 设备接入 tab 暂无查询筛选，保持腾讯云设备、萤石设备两个管理区块不变。
- 同步调整 `loading.tsx`，保持 tab 下方列表 Card 骨架和真实页面一致。

## 租户员工页整改

已将租户员工页调整为：

- 顶部保留员工管理标题、页面说明和新增员工主操作。
- 指标区展示当前筛选员工、本页在职、本页待入职、本页已绑定登录。
- 删除独立筛选 Card。
- 将员工状态筛选和关键词搜索放入“员工列表” Card 头部。
- 分页放入员工列表 Card 底部，和员工表格形成完整查询上下文。
- 同步调整员工 `loading.tsx` 使用的骨架组件，保持两段式骨架和真实页面一致。

## 租户组织架构页整改

已将租户组织架构页调整为：

- 顶部保留组织架构标题和页面说明。
- 指标区展示当前筛选部门、当前筛选岗位、项目候选规则、部门岗位规则。
- 保留部门、岗位、候选规则、部门岗位四个 tab，tab 仍作为业务分区入口，并去除 active tab 阴影。
- 部门 tab 删除独立筛选 Card，将部门编码和关键词搜索放入“部门列表” Card 头部。
- 部门 tab 分页放入部门列表 Card 底部，和部门表格形成完整查询上下文。
- 岗位 tab 删除独立筛选 Card，将岗位状态、薪资类型和关键词搜索放入“岗位列表” Card 头部。
- 岗位 tab 分页放入岗位列表 Card 底部，和岗位表格形成完整查询上下文。
- 候选规则、部门岗位 tab 暂无分页查询，保持配置型内容结构不变。
- 同步调整 `loading.tsx`，保持指标区、tab 和列表 Card 骨架与真实页面一致。

## 后续页面整改原则

- 筛选条件应该放在它作用的列表 Card 内，不应作为页面级独立模块。
- 页面顶部只放概览和关键动作，不放大段筛选表单。
- 列表页不要重复侧边栏入口。
- 指标区只展示对当前业务判断有帮助的数据。
- 表格、筛选、分页应作为一个完整查询上下文出现。
- `loading.tsx` 骨架屏必须和真实页面结构一致。真实页面是两段式时，骨架也必须是两段式；筛选骨架放在列表 Card 的 `CardHeader` 内，分页骨架放在列表 Card 底部，禁止保留独立筛选 Card 造成加载态和完成态跳变。

## 可复用页面

后续可按此模式整改：

- 项目管理
- 费用审批
- 员工管理
- 角色管理
- 平台线索
- 平台审计
