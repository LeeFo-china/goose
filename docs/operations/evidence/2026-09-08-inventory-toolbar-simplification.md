# 库存工具栏精简验收

## 变更范围

- 删除库存页顶部标题、副标题和独立仓库搜索，统一工具栏控件高度。
- 仓库选择复用本地 Radix 菜单单选组件；仅展开且多页时显示分页。
- 仓库目录仍每页 20 条，保留停用仓库、跨页选中名称、失败重试和空目录状态。
- 库存余额/流水、商品搜索、下钻、权限与表格分页不变。
- 仅修改 gooes Admin 及测试、文档；后端、数据库、orange 和生产配置未改。

## 本地验证

- 新增的“无标题”和“折叠仓库筛选无搜索/分页”测试先失败（4 pass / 2 fail），实现后通过。
- 库存 4 个单测文件分进程运行：20 pass、0 fail。
- `bun run check`：文件大小门禁、Next 类型生成、TypeScript 检查通过。
- `bun run build`：Admin 完整构建通过，95 个静态页面生成完成。
- 库存 Playwright：10 pass，覆盖列表及目录分页、权限、搜索、下钻、失效响应、重试、空目录、单页隐藏分页、跨页选择与键盘焦点。
- 已人工查看桌面、375px、展开菜单和长名称截图。窄屏目录可滚动、翻页、选择，文档不横向溢出。
- 当前 Admin 使用已有浅色语义主题，未新增深色主题；不把添加 dark 类视为深色主题验收。
- 独立只读代码审查无 Critical / Important 问题；建议补充的窄屏展开及长名称测试已落实。

截图由现有 harness 生成在 `apps/admin/test-results/inventory/`，使用纯本地 fixture，不代表真实租户数据联调。

## @gooes/domain

- 当前版本 `1.21.0`，本次没有领域源码、接口类型或依赖变更，不需要升版及发布新包。
- 已在 `packages/domain` 运行 `bun run build`，bundle、声明文件生成及 verify-build 均成功；146516 bytes，外部 zod 身份校验通过。
- 无 domain 或 lockfile 的版本控制改动。

## 提交与发布边界

用户授权当前 main 直接提交并 push。只提交本任务源码、测试、计划和本记录；既有 `.artifacts/` 保留且不提交。Push 将按现有工作流触发镜像构建，成功后自动部署开发环境，本记录不宣称该远端流程已完成，也不代表生产发布。
