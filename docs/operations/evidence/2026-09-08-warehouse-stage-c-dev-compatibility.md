# 阶段 C 开发库兼容性与备份恢复门禁

日期：2026-09-08；数据库采样时间 12:56:37（Asia/Shanghai）。固定候选 `5585923ee22b25800a5e3808411f3dab44be9fb5`。

## 结论

**已检查的迁移前置兼容条件通过，备份恢复门禁未通过，继续禁止 apply。**

本轮执行用户确认的只读真实库兼容性检查与现有备份核验。未应用任何 migration，未部署、推送、触发工作流、启用租户、创建新备份或恢复数据库；未修改 Orange。系统目录和数据核对在显式 `BEGIN READ ONLY` 内执行，返回 `transaction_read_only=on`，语句超时 20 秒、锁等待超时 3 秒。只返回结构信息及聚合数量，没有导出业务明细。

完整脱敏结果见 [兼容性与归档检查 JSON](2026-09-08-warehouse-stage-c-dev-compatibility.json)。先前完整 CLI 历史见 [迁移预检](2026-09-08-warehouse-stage-c-dev-migration-preflight.md)。

## 1. 兼容性检查

开发 PostgreSQL 17.6 的迁移历史仍为 594 条、最新 `20260908010000`，与此前完整历史逐条一致。候选仍为 598 条，缺少相同 4 条 C migration，没有 remote-only 版本。

| 迁移 | 实际前置检查 |
| --- | --- |
| C1 `20260908015230` | 待新增表、序列、索引、函数及列没有重名；待删除的 3 个来源约束存在且 validated；6 个待放宽采购来源列存在且为非空 UUID；既有复合外键引用目标的唯一约束存在；库存分页函数的精确替换点匹配 1 次 |
| C2 `20260908015649` | 4 个预算／风险函数均唯一，均包含成本来源表、尚无方向表达式；替换点按文件要求分别匹配 2／1／1／1 次 |
| C3 `20260908020216` | 现有 rollout 私有核心签名正确；按迁移顺序在内存中逐次替换，7 个旧片段均恰好匹配 1 次；新的 JSON 公开重载尚不存在 |
| C4 `20260908023924` | 新设置读取函数尚不存在；依赖的既有权限 helper 存在，新材料 actor helper 和开关列由 C1 先创建 |

函数匹配共 12 项，全部符合精确替换断言；没有在真实数据库执行函数替换或调用业务写 RPC。存在性及片段匹配不等于全部函数行为正确，也不是在真实数据副本上执行整套迁移后的编译／业务验收。

数据检查：

- `project_cost_events` 实际 4 条，全部满足迁移后保留的采购来源身份分支，异常 0 条。新方向默认 increase，新仓库来源字段默认 NULL。
- `inventory_transactions` 实际 0 条，来源兼容异常 0 条。
- `tenant_supplier_settings` 实际 1 条，module_enabled 空值 0 条；新材料开关默认 false，不会因加列开启租户。
- 当时超过 5 分钟的其他事务为 0，锁等待会话为 0；仅是采样结果，执行前必须重新检查。

## 2. 现有备份核验

只读取既有归档 `/var/tmp/gooes-rollout-dev-backup.09wscW/public-and-migrations.dump`：

- 归档时间：2026-09-08 01:28:52（Asia/Shanghai），早于本轮检查约 11.5 小时。
- 大小 8,036,593 bytes；目录权限 0700、文件权限 0600。
- SHA-256：`a61fb6061ba6ab1533dc1fc4613f9d307b4426b252a95977b97589a6e9ddf76c`，与原发布记录一致。
- 归档来源和当前服务器的 pg_dump／pg_restore 均为 PostgreSQL 17.6。
- `pg_restore --list` 可读：5,691 个归档项，275 个 TABLE DATA，包含 public 和 supabase_migrations。
- 仅从归档读取迁移历史，取得 593 条，最新 `20260907102914`。当前库为 594 条，**该备份不包含 `20260908010000`**。
- 使用 `pg_restore --file=-` 完整解码到仅计字节的管道，退出 0，共 20,867,035 字节；没有指定目标数据库、输出 SQL 到终端或落盘业务内容。此检查证明归档可完整读取，**不证明恢复到数据库成功**。

## 3. 尚未满足的恢复条件

1. 现有归档不是当前迁移前快照。恢复它会退回较早的 schema／历史，且本轮没有证明归档后业务数据未变化。
2. 归档仅覆盖 public 和 supabase_migrations，不包含 auth、storage 或全局角色；新建隔离数据库恢复前必须准备相关依赖，不能称为全实例备份。
3. 未发现本次归档的恢复演练证据，本轮也未执行恢复。解码成功不能取代约束、依赖、行数和迁移历史的实际恢复核验。
4. 实际 `archive_mode=off`、archive_command 未配置；没有 PostgreSQL WAL 归档型 PITR 的就绪证据。本机 timer 列表也未识别数据库备份定时器，但不据此断言外部备份或云快照不存在。
5. 开发机文件系统 59G、已用 53G（94%）、可用约 3.9G；数据库约 126.8 MB。不能单凭当前小库体积保证备份、恢复副本和部署镜像共用磁盘时安全，需明确空间预算及保留策略，不能擅自清理镜像、日志或旧备份。
6. 当前开发 migration 工作流未包含备份／恢复步骤；不能指望执行 apply 时自动补齐这些门禁。

## 下一步建议与授权边界

建议先准备当前开发库备份（明确恢复所需 schema／角色依赖及范围），在与真实业务隔离的数据库中做恢复演练，核对完整迁移历史和业务表数量，并确认空间预算／迁移窗口。新建备份、隔离恢复环境及可能的数据转移需另行确认；本轮没有执行或暗中创建这些资源。

上述条件通过后，仍须确认不可变发布提交／推送、运行开发工作流 plan、复核同一 4 条文件与 hash，再单独确认 apply。迁移后全量 Local／Remote 对齐；部署和实际租户开关仍分别受控。

迁移失败以前向修复为主，保留库存／成本不可变事实。不得用旧归档覆盖当前开发库来“测试恢复”，也不得在已有退料事实后回退到不识别成本方向的 API。
