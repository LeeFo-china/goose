# 抖音留资来源归因验收

## 发布顺序

先应用 `20260914202700_accept_douyin_official_lead_attribution.sql`，核对 `supabase migration list` 的 Local/Remote 对齐，再发布 API 和租户 Admin，最后上传目标抖音小程序体验版。旧 RPC 会拒绝新的 `analysis_info` 对象，因此不可先上传小程序。

如需回滚，先恢复上一版小程序和 API；保留已扩展的客户来源快照校验器，使带官方归因的既有预约仍可转为客户。只有核对这些不可变快照的后续转换路径后，才能通过新的前向 migration 收紧校验器，不能直接恢复旧白名单。

仓库 `apps/douyin-mini/project.config.json` 当前是模板 AppID `tt0d647bd99301341b01`；用户要验收的“好店智装云”体验版 AppID 为 `ttd033a68e4e56ccd301`。上传前必须在开发者工具确认实际登录与上传目标是后者。抖音开放平台须确认目标应用与用于测试的品牌号、员工号已绑定，真机基础库须达到 `tt.getAnalysisInfo` 要求的 2.99.0 或更高。此处均不能从代码推断为已满足。

## 真机用例

使用内部测试线索，iPhone 和 Android 各覆盖：绑定账号的短视频、直播、主页，以及直接打开、未绑定账号入口。每次从外部重新进入，完成一次量房申请，在租户 Admin 的客户线索详情核对“本次留资来源”。短视频应显示抖音号与加密视频 ID；直播应显示直播间 ID；主页应显示抖音号。直接打开和不支持入口应显示“未识别”，带 `campaign_code` / `content_id` 的链接只能显示“链接参数（手工标记）”。在同一小程序会话内切换来源、快速提交、重复提交同一幂等键，检查不串用前一次账号或视频，且重复结果仍指向原预约快照。

需要对比抖音官方返回值时，只在测试设备的开发者工具调用 `tt.getAnalysisInfo`，仅记录 `type`、`uniqueId`、`itemId`、`roomId`、`postId`、`anchorId` 与失败代码。不得记录手机号、token、签名 URL、家庭照片或原始完整响应。开发者工具模拟可验证页面与接口联通，不能替代真实视频入口的真机结果。

## 当前证据状态

代码与类型契约已通过本地自动化检查。目标 AppID 的账号绑定状态、基础库版本、真实入口返回值以及 iOS/Android 真机展示尚未取得证据；上线判断须等待这些用例的脱敏结果。
