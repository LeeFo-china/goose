# 城市合伙人审核交互修复

## 已确认范围

用户于 2026-09-06 确认执行：只补待审核合伙人的“审核通过并启用”，验证后合并本地 main，不推送、不发布生产。以 main 的 176ba328 为基线，不修改 orange 或此前未提交的扫码交接文档。

## 根因与方案

`platform-partner-tables.tsx` 的合伙人操作列仅连接区域编辑、邀请码按钮，漏接审核。后端 `PATCH /platform/partners/:id/status` 已存在，校验管理权限、员工身份及启用区域规则。

复用现有 MutationDialogButton，通过新审核按钮连接状态接口；不使用申请线索的 approve 接口（该接口会创建合伙人）。不扩展暂停、终止或驳回流程，不修改数据库。

## 交互契约

- 页面从 session.permissions 读取 platform.partner.manage，通过 table prop 传给审核按钮；仅管理权限且 status=pending 才显示“审核通过”。
- 弹窗标题、提交按钮为“审核通过并启用”，核对名称、联系人、手机号、区县。无区县不能提交，并提示先编辑区域。
- 审核说明必填，trim 后 1–300 字。明确提示现有接口会将说明更新为合伙人备注，不伪装为独立审计记录。
- 提交 PATCH，body 仅 `{status:"active",reason}`。同步防重入锁加 pending 禁用，取消不请求，提交中不可关闭。
- 服务端区域冲突、鉴权等失败展示原错误，保留弹窗和说明，允许修正后重试；不绕过后端校验。
- 成功关闭并刷新原列表，维持 URL 分页/筛选；非待审核记录不再显示审核按钮。
- 沿用后端当前状态接口，不引入新的状态机/并发协议；不声称跨会话幂等或版本校验。

## 验证及边界

先运行现有合伙人测试，新增失败用例证明缺少入口；然后补实现并跑类型检查、文件大小检查、合伙人测试及 loopback-only Playwright。

浏览器覆盖：成功及刷新、取消、无权限/非 pending、空说明、最大长度、区域未配、区域冲突保留输入、防重复提交和窄屏弹窗。所有业务写入仅发送到本地 mock，无生产数据操作。
